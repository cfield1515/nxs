let userToken = null;

async function authenticate() {
  // Example placeholder — replace with your real auth flow
  userToken = "fake-user-token";
  document.getElementById("status").innerText = "Authenticated ✅";
}

authenticate();
